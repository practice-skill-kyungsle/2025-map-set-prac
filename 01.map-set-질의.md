# 01. 맵은 이런 식으로 이중배열로 만드는거야?

네, 맞습니다! Map을 생성할 때 **이중 배열**(배열 안에 [키, 값] 형태의 배열들)을 전달할 수 있어요.


```javascript
// 방법 1: 빈 Map 생성 후 set으로 추가
const map1 = new Map();
map1.set('a', 1);
map1.set('b', 2);
map1.set('c', 3);

// 방법 2: 생성 시 이중 배열로 초기화
const map2 = new Map([
  ['a', 1],
  ['b', 2],
  ['c', 3]
]);

// 둘 다 결과는 동일합니다!
```

이중 배열의 구조는 이렇게 이해하시면 됩니다:

```javascript
[
  [키1, 값1],
  [키2, 값2],
  [키3, 값3]
]
```

각 내부 배열은 정확히 2개의 요소를 가져야 해요:
- 첫 번째 요소: 키
- 두 번째 요소: 값

```javascript
// 다양한 타입의 키-값 예시
const map = new Map([
  ['name', '철수'],           // 문자열 키
  [1, 'one'],                 // 숫자 키
  [true, 'boolean key'],      // 불린 키
  [{id: 1}, 'object key']     // 객체 키
]);
```

다른 궁금한 점 있으세요?